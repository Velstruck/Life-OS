# API URL for the backend (without /api suffix)
# For local development
VITE_API_URL=http://localhost:3000

# For production, replace with your deployed backend URL (without /api suffix)
# VITE_API_URL=https://your-backend.vercel.app
